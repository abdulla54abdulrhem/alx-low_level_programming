#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point
 *
 * Return:always return 0
 *
 */
int main(void)
{
int a;

for(a = 1; a <= 9; a++)
{
putchar(a);
}
putchar('\n');
return (0);
}
